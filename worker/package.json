{
  "name": "search-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:create": "wrangler d1 create search-db",
    "d1:migrate": "wrangler d1 migrations apply search-db"
  },
  "dependencies": {
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "wrangler": "^4.80.0"
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply search-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply search-db --remote"
  },
  "dependencies": {
    "hono": "^4.12.4"
  },
  "devDependencies": {
    "wrangler": "^4.59.1"
  }
}
