name: Deploy Stage

on:
  pull_request:
    branches: [main]

jobs:
  deploy-stage:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      deployments: write
      pull-requests: write
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - run: npm run build
        env:
          API_BASE: ${{ secrets.WORKER_URL }}

      - name: Create Cloudflare Pages project (if not exists)
        run: npx -y wrangler pages project create search-dashboard --production-branch=main
        continue-on-error: true

      - name: Deploy to Cloudflare Pages (stage)
        run: npx -y wrangler pages deploy www --project-name=search-dashboard --branch=${{ github.head_ref }}
